import {transform_for_map} from "./map";

export function transform_data (data, transformation_type) {
    return data
    // switch(transformation_type) {
    //     case 'loc_map':
    //         return transform_for_map(data)
    //     case 'loc_table':
    //         return data
    // }
}

